%Activity 3.3 (10pts) Write a function that accepts temperature in degrees Fahrenheit 
%(oF) and computes the corresponding value in degree Celsius (oC). The relation between the two is 
%function header
function c=ActivityThreeThree(f)
%compute

c=(5/9)*(f-32);

%end
end
%function check
